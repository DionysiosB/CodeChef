#include <cstdio>

int main(){

    long t; scanf("%ld", &t);
    while(t--){
        int len; scanf("%d", &len);
        while(len--){int d; scanf("%d", &d); printf("%d", d);}
        puts("");
    }

    return 0;
}
